import { TiempoTranscurridoPipe } from './tiempo-transcurrido.pipe';
describe('Pipe: TiempoTranscurridoe', () => {
  it('create an instance', () => {
    let pipe = new TiempoTranscurridoPipe();
    expect(pipe).toBeTruthy();
  });
});
